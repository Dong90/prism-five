# Examples

示例项目演示如何串联使用所有五个棱镜。

## 计划

- `yoga-landing/` — 完整的端到端示例：事件的接收、变换、分发、收集与输出
- `event-pipeline/` — 事件管道，从 WebSocket 到文件持久化

## 运行示例

```bash
cd examples/yoga-landing
npm install
npm start
```
